// Concatena due stringhe.

const str1 = `Ciao sono Filippo`
const str2 = `e vivo a Palermo`

const sumStr = (str1, str2) => {
    const space = ` `;
    return console.log(str1 + space + str2);
};

sumStr(str1, str2)